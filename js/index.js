// Prevent us from attempting to use variables
// that are not declared
"use strict"
// Array of numbers to loop through
/*const numbers = [3,12,14,20, 24,26, 33, 45, 51, 67, 72, 89, 90, 120, 163];

// Loop through the array
for (let i = 0; i < numbers.length; i++) {
    const number = numbers[i];

    // Skip numbers divisible by 3
    if (number % 3 === 0) {
        console.log(`Skipping ${number} because it's divisible by 3.`);
        continue; // Skip the rest of the loop and move to the next number
    }

    // If the number is greater than 50, exit the loop
    if (number > 50) {
        console.log(`Stopping the loop because ${number} is greater than 50.`);
        break; // Break the loop and stop further execution
    }

    // This code runs if none of the conditions are met
    console.log("Processing number: ", numbers[i]);
}*/
//
// Array of months of the year for stab 2
//
//
/*const months = ["January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December"];

// Loop through the array of months
for (let i = 0; i < months.length; i++) {
    const month = months[i];

    // Check if the month has the letter "r" (suitable for eating oysters)
    if (!month.includes("r")) {
        console.log(`${month} is not a month to eat oysters. Go Vegan! Skipping...`);
        continue; // Skip to the next month
    }

    // If we encounter a month after skipping unsuitable ones, we print the message
    console.log(`${month} is a month to eat oysters! But why would you? Go Vegan!`);

    // If it's December, we'll break the loop to stop further processing
    if (month === "December") {
        console.log("It's December! Stopping the loop.");
        break; // Exit the loop entirely
    }
}*/

